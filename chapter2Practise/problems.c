#include <stdio.h>

int main() {
    
    /*
        1. Which of the following is invalid in C?
            a. int a=1; int b = a;  true
            b. int v = 3*3;  true
            c. char dt = ‘21 dec 2020’; false
        2. What data type will 3.0/8 – 2 return?    float
        3. Write a program to check whether a number is divisible by 97 or not.
        ans : 
        #include <stdio.h>
        int main() {
            
        
            int num;
            printf("Enter a number: ");
            scanf("%d", &num);
            if (num % 97 == 0) {
                printf("%d is divisible by 97\n", num);
            }
            else {
                printf("%d is not divisible by 97\n", num);
            }



            return 0;
        }



        4. Explain step by step evaluation of 3*x/y – z+k, where x=2, y=3, z=3, k=1
        ans:    
        // Given values
        int x = 2, y = 3, z = 3, k = 1;
        // Step 1: Calculate 3*x
        int step1 = 3 * x; // 3 * 2 = 6
        // Step 2: Calculate step1/y
        int step2 = step1 / y; // 6 / 3 = 2
        // Step 3: Calculate step2 - z
        int step3 = step2 - z; // 2 - 3 = -1
        // Step 4: Calculate step3 + k
        int result = step3 + k; // -1 + 1 = 0
        printf("Result of 3*x/y - z + k = %d\n", result); // Output: 0
        


        5. 3.0 + 1 will be:
        a. Integer.
        b. Floating point number.  (answer)
        c. Character.
    
    
    */



    return 0;
}